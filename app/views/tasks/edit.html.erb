<%= form_for @task do |f| %>
  <%= f.label  :name, "enter a task" %>
  <%= f.text_field :name %>
  <%= f.label  :done, "is it already done ?" %>
  <%= f.check_box :done %>
  <%= f.submit "submit", class: "btn btn-primary" %>
<% end %>
